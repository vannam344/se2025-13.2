'use strict';

/** @type {import('sequelize-cli').Migration} */
module.exports = {
    async up() {
        return Promise.resolve();
    },

    async down() {
        return Promise.resolve();
    },
};
